# Web Configuration (Next.js app)
# URLs for connecting to the server
NEXT_PUBLIC_SERVER_URL=http://localhost:8787
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:8787

# Note: Server-side authentication with Next.js and better-auth
# is not fully implemented in this app. Only client-side authentication works.